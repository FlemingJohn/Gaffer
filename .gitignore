# Dependencies
node_modules/

# Build output (Vite frontend)
dist/
web/dist/
.vite/

# Environment
.env

# QVAC model cache + downloaded weights (gigabytes — never commit)
.qvac/
models/cache/
*.gguf
# ...except the trained team LoRA adapter (~34 MB) — shipped so judges can run
# `npm run demo:team` without a 30-min retrain. Checkpoint .gguf files stay out.
!data/adapters/trained-lora-adapter.gguf
data/adapters/checkpoints/

# Runtime scratch: diffusion probes, TTS wavs, live outputs, rebuildable RAG index
# (rag-index.json is regenerated by `npm run ingest`).
data/cache/


# Logs / runtime
*.log
logs/
finetune.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
